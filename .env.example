NEXT_PUBLIC_API_URL=http://localhost:5000

# Cloudinary unsigned upload (used by the tournament organizer's cover
# image upload) — create a free account, an unsigned upload preset, and
# fill these in on your own .env.local (never commit real values here).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
